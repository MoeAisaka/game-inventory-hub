# Contributing

Use migrations for schema changes, validate request and connector data at runtime, preserve manual-data precedence, and add tests for authorization and state transitions. Never submit real user libraries or connector credentials.
