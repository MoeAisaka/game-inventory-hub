{
  "name": "game-inventory-playstation-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isolated read-only PlayStation snapshot adapter for Game Inventory",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preview": "node src/main.mjs --preview",
    "submit": "node src/main.mjs --submit",
    "test": "node --test"
  },
  "dependencies": {
    "psn-api": "2.18.0"
  }
}
